# Explorer Report — Terceira coluna para visualização do resumo

**Data:** 27-05-2026  
**Task:** Adicionar terceira coluna ao layout para exibir resumo em modo leitura dedicado

---

## Layout atual

O `App.tsx` usa um `<main>` com `grid grid-cols-1 lg:grid-cols-2`. Abaixo de `lg` (1024px), as colunas empilham verticalmente. O layout tem duas colunas hoje:

- **Coluna esquerda** (`lg:border-r`): gravação, transcrição — contém `ModelSelector` (transcrição), `FileUpload`, "Paste text" (textarea colapsável), botão de gravação, erros, e `TranscriptionView`
- **Coluna direita**: resumo/documento — contém `ModelSelector` (resumo), `SummaryView` e `SaveButton`

Não há `overflow` ou `height` fixo no `<main>` — o conteúdo cresce livremente e a página rola verticalmente.

---

## SummaryView — modo de leitura ou edição?

O `SummaryView` não tem modo de leitura. O resumo é exibido exclusivamente em um `<textarea>` (`min-h-[320px]`, `resize-y`, `font-mono`). Não existe renderização Markdown nem modo read-only — é só um campo de texto editável.

Para a terceira coluna com "tipografia otimizada para leitura contínua", será necessário criar uma nova camada de exibição (provavelmente `<div>` com renderização de Markdown ou ao menos formatação de parágrafos/seções). O textarea pode permanecer na coluna de controles/edição ou ser removido do fluxo principal.

---

## Props e estado entre App.tsx e SummaryView.tsx

```
transcription: string         — texto base para geração
selectedSummaryModel: string | null — modelo selecionado
summary: string               — estado controlado por App.tsx (setSummary)
onSummaryChange: (text) => void — callback que atualiza summary no App
```

O `summary` vive em `App.tsx` como `useState`. `SummaryView` recebe e modifica via callback. `SaveButton` também consome `summary` diretamente do App. Não há estado interno de resumo fora do App — arquitetura limpa para extrair a exibição de leitura.

---

## O que facilita a migração

1. **Estado centralizado no App:** `summary` já é controlado pelo App, então uma terceira coluna que apenas exibe `summary` sem precisar de callback próprio é trivial — só precisa da prop `summary: string`.
2. **Grid CSS simples:** a troca de `lg:grid-cols-2` para `lg:grid-cols-3` é uma linha. A distribuição proporcional pode ser feita com `grid-cols-[auto_1fr_1fr]` ou similar.
3. **Componentes bem separados:** cada coluna já tem limites claros de responsabilidade. O `SummaryView` não tem lógica acoplada ao layout — é fácil partir ele em duas partes (controles de geração + área de leitura) sem quebrar nada.
4. **Padrão visual consistente:** cores, bordas e fontes estão bem padronizadas nos tokens Tailwind usados no projeto (`[#0e0a1a]`, `[#1f1a36]`, `[#2d2650]`, `violet-*`). A terceira coluna pode seguir o mesmo padrão sem overhead.

---

## O que complica a migração

1. **Sem renderização Markdown:** o resumo gerado pelo modelo já usa Markdown (`## Summary`, `## Key Points` etc. conforme o `DEFAULT_SYSTEM_PROMPT`). Exibir isso como "leitura contínua" de forma atraente requer ou um parser Markdown (ex: `react-markdown`) ou uma formatação manual por regex/split. É a parte com maior trabalho.
2. **SummaryView mistura controles e área de resultado:** o componente atual une num único bloco: prompt colapsável, textarea de prompt adicional, toggle modo, botão gerar, erro, e textarea de resultado. Para separar a exibição em coluna própria, o componente precisará ser refatorado ou um novo componente de leitura criado. Não é complexo, mas exige cuidado para não quebrar o fluxo de geração.
3. **Responsividade de 3 colunas:** 3 colunas em janelas estreitas é mais sensível que 2. Em `lg` a tela pode ter 1024px dividido em 3 — ~341px por coluna. Dependendo do conteúdo dos controles (FileUpload com padding generoso, ModelSelector), pode ficar apertado. O breakpoint pode precisar ser ajustado para `xl` (1280px), ou as proporções definidas de forma assimétrica (coluna de controles mais estreita).
4. **TranscriptionView ainda na coluna esquerda:** a task define a coluna 1 como só "controles" e coluna 2 como "transcrição". Hoje transcrição e controles estão misturados na mesma coluna. Separar isso é um passo a mais além da terceira coluna — implica mover `TranscriptionView` para uma coluna dedicada, e os controles (ModelSelector, FileUpload, botões) para a primeira.

---

## Nível de dificuldade

**Média**

A estrutura é simples e bem organizada, mas há dois pontos que aumentam o esforço: (1) a necessidade de renderizar Markdown de forma legível na terceira coluna, e (2) a reorganização da coluna esquerda para separar controles de transcrição — o que vai além de só "adicionar uma coluna".

---

## Recomendações para o plannerAgent

1. **Instalar `react-markdown`** (ou equivalente leve) para renderizar o resumo na terceira coluna. Alternativa sem dependência: split por `\n\n` e detecção de headings `##` para formatar seções manualmente — funciona bem dado o formato fixo do `DEFAULT_SYSTEM_PROMPT`.

2. **Planejar a reorganização das 3 colunas:**
   - Coluna 1 (controles): `ModelSelector` (transcrição + resumo), `FileUpload`, "Paste text", botão gravar, erros
   - Coluna 2 (transcrição): `TranscriptionView` + controles de geração do resumo (`SummaryView` sem a área de exibição)
   - Coluna 3 (leitura): novo componente `SummaryReadView` que exibe `summary` renderizado, com placeholder e estado de loading

3. **Refatorar `SummaryView`** em dois: `SummaryControls` (prompt, botão gerar, erro) para coluna 2, e `SummaryReadView` (exibição renderizada) para coluna 3. Manter a interface de props compatível com o App.

4. **Breakpoint:** testar se `lg:grid-cols-3` (1024px) é suficiente ou se `xl` (1280px) é mais adequado para não comprimir demais. Sugerir distribuição assimétrica: `grid-cols-[280px_1fr_1fr]` ou `grid-cols-[1fr_1.5fr_1.5fr]` — controles menores, colunas de texto maiores.

5. **Edge cases a cobrir:** placeholder na coluna 3 antes de gerar ("The summary will appear here"), spinner durante `isLoading`, scroll quando resumo for longo (`overflow-y-auto` com altura relativa ao viewport).

6. **Responsividade:** definir ordem de empilhamento: controles → transcrição → resumo (de cima para baixo em mobile/janela estreita). Usar `order-` ou manter a ordem natural do DOM.
