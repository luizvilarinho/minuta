# Task: Volp — Rebrand + Globalização + Input de Texto + Persistência de Prompt

## Objective
Transformar o app Minuta em um resumidor genérico chamado Volp. Isso envolve: renomear o app, traduzir todos os textos visíveis ao usuário e system prompts para inglês, persistir o prompt adicional no localStorage, e adicionar suporte a colar/digitar texto diretamente como fonte de entrada (sem precisar de arquivo de áudio).

## Context
Projeto Tauri 2.0 + React + TypeScript + Tailwind. Frontend em `src/`. As mudanças são todas no frontend + configurações do Tauri. O backend Rust não precisa de alterações — as chamadas de API já estão generalizadas (transcribe_audio, generate_summary).

Arquivos principais afetados:
- `src/App.tsx` — nome do app, textos da UI, labels das colunas, botão de reset
- `src/components/SummaryView.tsx` — system prompt (PT→EN), labels, botão, persistência do additionalPrompt
- `src/components/FileUpload.tsx` — textos de upload
- `src/components/Settings.tsx` — textos de configurações
- `src/components/TranscriptionView.tsx` — labels/placeholders
- `src/components/SaveButton.tsx` — textos do botão de salvar
- `src/components/ModelSelector.tsx` — labels
- `src-tauri/tauri.conf.json` — title da janela

## Requirements
1. **Rename:** "Minuta" → "Volp" em todos os textos visíveis e no título da janela Tauri
2. **Globalização (EN):** Todos os textos da UI e o DEFAULT_SYSTEM_PROMPT traduzidos para inglês. Strings de localStorage (`minuta_transcription_model`, `minuta_summary_model`) também renomeadas para `volp_*`
3. **Persistir prompt adicional:** O campo `additionalPrompt` em `SummaryView.tsx` deve ser salvo/carregado via `localStorage` com chave `volp_additional_prompt`. O promptMode também deve ser persistido com chave `volp_prompt_mode`
4. **Colar texto para resumo:** Adicionar na coluna esquerda, abaixo do FileUpload, um campo de texto (textarea) onde o usuário pode colar ou digitar texto diretamente. Ao ter conteúdo, esse texto é usado como transcrição — exatamente como se tivesse vindo de um arquivo TXT. Deve ser mutuamente exclusivo com a transcrição carregada (limpar um limpa o outro). Visualmente deve ser colapsável ou estar numa área clara delimitada

## Constraints
- Não alterar nenhuma lógica de backend (Rust) nem as chamadas `invoke`
- Não quebrar funcionalidades existentes: upload de arquivo, gravação, resumo, salvar
- Manter tema escuro e Tailwind como base visual
- Chave localStorage para modelos deve migrar de `minuta_*` para `volp_*` (sem backward compat — reset limpo)
- O DEFAULT_SYSTEM_PROMPT deve ser genérico para qualquer tipo de texto, não apenas reuniões

## Edge Cases
- Se o usuário tem texto colado E uma transcrição de arquivo/gravação, a mais recente sobrescreve (comportamento atual de `setTranscription`)
- Campo de texto colado deve limpar quando o usuário clicar em "New session" (reset)
- Se o localStorage tiver chaves antigas `minuta_*`, elas serão ignoradas — usuário precisará reconfigurar o modelo (comportamento esperado)
- O promptMode persistido deve ter valor padrão `"sum"` se não encontrado no localStorage
