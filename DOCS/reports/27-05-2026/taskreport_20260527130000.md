# Task: Mover botão New session para o header principal

## Objective
Mover o botão "New session" do cabeçalho da Col 1 para o header global do app, ao lado do botão Settings. Isso libera espaço no cabeçalho da Col 1 para que o texto "Recording & Transcription" respire melhor.

## Context
- `src/App.tsx` — único arquivo afetado
- Header global: linhas 149–169 (logo ◈ Volp à esquerda, Settings à direita)
- Cabeçalho da Col 1: linhas 178–195 (ícone + "Recording & Transcription" + "New session" com justify-between)

## Requirements
- Botão "↺ New session" sai do cabeçalho da Col 1 e vai para o header global, ao lado do botão Settings
- Col 1 perde o `justify-between` e o botão, ficando só o ícone + título
- Comportamento (onClick → handleReset) permanece idêntico

## Constraints
- Apenas `src/App.tsx` — alteração mínima
- Manter estilo visual consistente com o botão Settings existente
